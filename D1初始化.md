# MoonTV 本地 D1 测试环境

本地 D1 由 Wrangler/Miniflare 模拟，数据保存在 `.wrangler/state/v3/d1/`。该目录已加入 `.gitignore`，不会上传到仓库，也不会连接或修改生产 D1。

## 首次准备

```bash
pnpm install
pnpm d1:local:prepare
```

`d1:local:prepare` 会依次执行仓库中的版本化迁移，并验证 7 张业务表是否齐全。迁移文件位于 `migrations/`，可以重复执行。

## 启动本地 D1 版本

```bash
pnpm d1:local:dev
```

该命令会：

1. 通过统一的 `cloudflare:build` 以 `NEXT_PUBLIC_STORAGE_TYPE=d1` 构建 OpenNext Worker；
2. 应用本地 D1 迁移；
3. 验证数据库结构；
4. 在 `http://localhost:8787` 启动本地 Cloudflare Worker。

普通的 `pnpm dev` 仍使用默认 `localstorage`，运行在 `http://localhost:3000`，两种开发模式互不覆盖。

## 单独检查数据库

```bash
pnpm d1:local:verify
```

检查只返回表结构状态，不打印用户名、密码摘要或其他业务数据。

## 重置本地测试数据

重置只删除 `.wrangler/state/v3/d1/`，不会操作 Cloudflare 生产 D1。为防误删，必须输入固定确认词：

```bash
pnpm d1:local:reset -- --confirm moonvideo-local
```

重置后脚本会自动重新执行迁移并验证表结构。没有确认词时脚本会拒绝执行。

## 数据库结构

版本化真源是：

- `migrations/0001_initial.sql`

当前包含：

- `users`：用户及密码摘要；
- `admin_config`：站点、权限和视频源配置；
- `auth_rate_limits`：登录限流；
- `play_records`：播放进度；
- `favorites`：收藏；
- `search_history`：搜索历史；
- `skip_configs`：片头片尾跳过设置。

新增或修改表结构时，不要直接修改已经发布的迁移；应新增后续编号迁移，例如 `migrations/0002_xxx.sql`。

## 安全边界

- 本地命令全部带 `--local`，不会访问生产 D1；
- 所有 Cloudflare 构建统一固定为 D1 模式，避免部署包退回浏览器本地存储；
- 仓库不保存测试用户密码、Cloudflare Token 或 API Key；
- 测试账号应通过页面临时创建，不写进迁移；
- 任何生产迁移都必须单独审核并明确使用 `--remote`，不得把本地命令改成远程命令。
